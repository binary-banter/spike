fn div(n: Int) -> Int {
    (/ n 2)
}

fn mul(n: Int) -> Int {
    (+ 1 (* 3 n))
}

fn col(n: Int) -> Bool {
    (if (== n 1) t (let (fun (if (== (% n 2) 0) div mul)) col((print fun(n)))))
}

fn main() -> Int {
    (if col((read)) 0 1)
}